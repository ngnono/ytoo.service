@model InboundPackViewModel
@{
    ViewBag.Title = "订单拒收";
    string orderNo = ViewBag.OrderNo;
}
@section PageBread
{
    <ul class="breadcrumb">
        <li>@Html.ActionLink2("订单管理", "Index")<span class="divider">/</span></li>
        <li>@Html.ActionLink2("订单详情", "Details", new { OrderNo = orderNo})<span class="divider">/</span></li>
        <li class="active">@ViewBag.Title</li>
    </ul>
}

<h2>@ViewBag.Title</h2>
@using (Html.BeginForm("Reject", "Order", new { OrderNo = orderNo}, FormMethod.Post))
{
    @Html.ValidationSummary(false)
        <div class="editor-label">@Html.LabelFor(model => model.ShippingVia)</div>
     <div class="editor-field">
             @Html.DropDownListFor(model=>model.ShippingVia,OrderViewModel.SelectedShipping)
     </div>
            <div class="editor-label">@Html.LabelFor(model => model.ShippingNo)</div>
     <div class="editor-field">
            @Html.EditorFor(model=>model.ShippingNo)
     </div>
     <div class="form-actions">
        <input type="submit" value="保存" class="btn btn-primary" />
        @Html.ActionLink2("返回订单", "Details", new { OrderNo = orderNo}, new { @class = "btn" })
 </div>
}

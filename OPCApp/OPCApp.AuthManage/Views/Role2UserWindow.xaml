<UserControl x:Class="OPCApp.AuthManage.Views.Role2UserListWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" Width="653.3" Height="460"
     >
    <Grid Margin="0,0,18,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="57*"/>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="58.209"/>
            <RowDefinition Height="70.791"/>
            <RowDefinition Height="Auto" MinHeight="194"/>
        </Grid.RowDefinitions>
        
        <DataGrid HorizontalAlignment="Left" AutoGenerateColumns="True" Grid.Column="0" Margin="10,1,0,-67" Width="235" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="全选" Width="50" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="角色名称" Width="80" Binding="{Binding ShopId}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Left" AutoGenerateColumns="True" VerticalAlignment="Top" Width="271" Height="331" Grid.Row="2" Grid.Column="1" Margin="0,0,0,-77">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="全选" Width="50" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="用户名称" Width="80" Binding="{Binding ShopId}"/>
                <DataGridTextColumn Header="所属角色名称" Width="80" Binding="{Binding ShopId}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

<UserControl x:Class="OPCApp.TransManage.Views.PrintInvoice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             Height="AUTO" Width="AUTO" Background="LightGray">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" Background="LightGray">

                <Button Background="LightGray" Width="50">
                    <StackPanel>
                        <Image Source="/image/变更单位.png" Stretch="None" />
                        <TextBlock HorizontalAlignment="Center">返回</TextBlock>
                    </StackPanel>
                </Button>
                <Button Background="LightGray" Width="50" Command="{Binding CommandSearch}">
                    <StackPanel>
                        <Image Source="/image/查询.png" Stretch="None" />
                        <TextBlock HorizontalAlignment="Center">查询</TextBlock>
                    </StackPanel>
                </Button>
                <Button Background="LightGray" Command="{Binding CommandViewAndPrint}">
                    <StackPanel>
                        <Image Source="/image/打印.png" Stretch="None" />
                        <TextBlock HorizontalAlignment="Center">打印预览</TextBlock>
                    </StackPanel>
                </Button>
                <Button Background="LightGray">
                    <StackPanel>
                        <Image Source="/image/打印.png" Stretch="None" />
                        <TextBlock HorizontalAlignment="Center">打印销售单</TextBlock>
                    </StackPanel>
                </Button>

                <Button Background="LightGray" Command="{Binding CommandFinish}">
                    <StackPanel>
                        <Image Source="/image/打印.png" Stretch="None" />
                        <TextBlock HorizontalAlignment="Center">完成打印销售单</TextBlock>
                    </StackPanel>
                </Button>

            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Background="LightGray">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="   购买时间：" />
                <DatePicker Width="150" Foreground="Blue" SelectedDate="{Binding Invoice4Get.StartSellDate}" />
                <TextBlock Text="  至：" />
                <DatePicker Width="150" Foreground="Blue" SelectedDate="{Binding Invoice4Get.EndSellDate}" />
                <TextBlock Text="  订单号" />
                <TextBox Width="150" Foreground="Blue" Background="White" Text="{Binding Invoice4Get.OrderNo}" />
                <TextBlock Text="  销售单" />
                <TextBox Width="150" Foreground="Blue" Background="White" Text="{Binding Invoice4Get.SaleOrderNo}" />
                <Button Content="查询" Width="60" Command="{Binding CommandSearch}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" Background="LightGray">
                <TextBlock Text="   销售单" />
            </StackPanel>
        </StackPanel>


        <DataGrid Grid.Row="3" ItemsSource="{Binding SaleList}" IsReadOnly="True" AutoGenerateColumns="False"
                  HorizontalScrollBarVisibility="Auto" SelectedItem="{Binding Invoice4Remark}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="选中" SortMemberPath="IsSelected" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Path=IsSelected,UpdateSourceTrigger=PropertyChanged}"
                                      Command="{Binding Path=DataContext.CommandGetDown,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type DataGrid}}}"
                                      VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="销售单号" Binding="{Binding Name}" />
              
                <DataGridTextColumn Header="订单号" Binding="{Binding OrderNo}" />
                <DataGridTextColumn Header="销售单号" Binding="{Binding SaleOrderNo}" />
                <DataGridTextColumn Header="销售类别" Binding="{Binding SalesType}" />
                <DataGridTextColumn Header="销售状态" Binding="{Binding ShipViaId}" />
                <DataGridTextColumn Header="销售单状态" Binding="{Binding StatusName}" />
                <DataGridTextColumn Header="收银状态" Binding="{Binding CashStatus}" />
                <DataGridTextColumn Header="销售时间" Binding="{Binding SellDate}" />
                <DataGridTextColumn Header="是否调拨" Binding="{Binding IfTrans}" />
                <DataGridTextColumn Header="调拨状态" Binding="{Binding TransStatus}" />
                <DataGridTextColumn Header="销售单金额" Binding="{Binding SalesAmount}" />
                <DataGridTextColumn Header="销售单数量" Binding="{Binding SalesCount}" />
                <DataGridTextColumn Header="门店" Binding="{Binding SectionName}" />
                <DataGridTextColumn Header="收银流水号" Binding="{Binding CashNum}" />
                <DataGridTextColumn Header="收银时间" Binding="{Binding CashDate}" />
                <DataGridTextColumn Header="专柜码" Binding="{Binding ShippingCode}" />
                <DataGridTemplateColumn Header="备注录入">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="录入"
                                    Command="{Binding Path=DataContext.CommandSetRemark,RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>


        <StackPanel Grid.Row="4">
            <StackPanel Orientation="Horizontal" Background="LightGray">
                <TextBlock Text="   销售明细" />
            </StackPanel>
        </StackPanel>
        <DataGrid Grid.Row="5" ItemsSource="{Binding InvoiceDetail4List}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="款号" Binding="{Binding SectionNumber}" />
                <DataGridTextColumn Header="规格" Binding="{Binding Standard}" />
                <DataGridTextColumn Header="色码" Binding="{Binding ColorCode}" />
                <DataGridTextColumn Header="销售价" Binding="{Binding SalePrice}" />
                <DataGridTextColumn Header="销售数量" Binding="{Binding SaleCount}" />
                <DataGridTextColumn Header="已退货数量" Binding="{Binding RMACount}" />
                <DataGridTextColumn Header="品牌" Binding="{Binding Brand}" />
                <DataGridTextColumn Header="吊牌价" Binding="{Binding OriginalPrice}" />
                <DataGridTextColumn Header="现价" Binding="{Binding PresentPrice}" />
                <DataGridTextColumn Header="备注" Visibility="Hidden" Binding="{Binding SalesAmount}" />
                <DataGridTextColumn Header="促销价格" Binding="{Binding SalesPrice}" />
                <DataGridTextColumn Header="商品编码" Binding="{Binding GoodsCode}" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>